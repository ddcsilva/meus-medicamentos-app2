# =============================================================================
# Meus Medicamentos - Cloud Functions - Variáveis de Ambiente
# =============================================================================
# Copie este arquivo para .env e configure os valores necessários.
# NUNCA commite o arquivo .env com credenciais reais!
# =============================================================================

# -----------------------------------------------------------------------------
# Configurações de Notificação por Email (futuro)
# -----------------------------------------------------------------------------
# Serviço de email a ser utilizado (sendgrid, mailgun, ses, smtp)
EMAIL_SERVICE=sendgrid

# API Key do serviço de email
EMAIL_API_KEY=your-email-api-key

# Email remetente
EMAIL_FROM=noreply@meusmedicamentos.com

# Nome do remetente
EMAIL_FROM_NAME=Meus Medicamentos

# -----------------------------------------------------------------------------
# Configurações de Notificação Push (futuro)
# -----------------------------------------------------------------------------
# Chave do servidor FCM (Firebase Cloud Messaging)
# FCM_SERVER_KEY=your-fcm-server-key

# -----------------------------------------------------------------------------
# Configurações de Agendamento
# -----------------------------------------------------------------------------
# Timezone para jobs agendados
TIMEZONE=America/Sao_Paulo

# Horário do job diário de verificação (formato cron: minuto hora)
DAILY_CHECK_HOUR=8
DAILY_CHECK_MINUTE=0

# Dia do mês para revisão mensal (1-28)
MONTHLY_REVIEW_DAY=1

# -----------------------------------------------------------------------------
# Configurações de Limites
# -----------------------------------------------------------------------------
# Dias antes da validade para alertar (padrão: 30)
DAYS_BEFORE_EXPIRY_WARNING=30

# Dias antes da validade para alerta crítico (padrão: 7)
DAYS_BEFORE_EXPIRY_CRITICAL=7

# Quantidade mínima padrão para alerta de estoque baixo
DEFAULT_LOW_STOCK_THRESHOLD=5


